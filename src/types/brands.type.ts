export interface brandsType {
  data: [
    {
      createdAt: string;
      image: string;
      name: string;
      slug: string;
      updatedAt: string;
      _id: string;
    },
  ];
  results: number;
}
